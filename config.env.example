# Example config.env for Render
# Postgres DB URL (Render sets it as DATABASE_URL)
DATABASE_URL=postgres://username:password@host:port/dbname

# Optional local bot name
BOT_NAME=charlie tech bot

# Optional panel access token (set a random string)
PANEL_TOKEN=somesecrettoken

# Note: Session credentials are stored in Postgres automatically
# Connect via /panel using:
# 1. QR Code — scan with WhatsApp device
# 2. Pairing Code (OTP) — enter 6-digit code in WhatsApp
# 3. Saved Session — upload your creds.json
